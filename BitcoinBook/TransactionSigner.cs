using System;

namespace BitcoinBook
{
    public class TransactionSigner
    {
        ScriptClassifier scriptClassifier;
        SignerMap signerMap;

        public Transaction SignTransaction(Wallet wallet, Transaction transaction)
        {
            throw new NotImplementedException();
        }
    }
}
